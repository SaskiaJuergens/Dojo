var name; //konstante für Hover und vorschlagen von Teilnehmern
var vorname;
var geburstag;
var alter;
var graduierung; //drop down
var geschlecht; //drop down
var nationalität;
var verein;

var playerData = []; //Eventhandler für HTML

// Beim Laden der Webseite wird die Funktion setup() aufgerufen
window.addEventListener("load", setup);
function setup() {
    showTeilnehmer();
}

function showTeilnehmer(){

}